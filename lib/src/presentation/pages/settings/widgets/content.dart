import 'package:flutter/material.dart';

class SettingsPageContentWidget extends StatelessWidget {
  const SettingsPageContentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
